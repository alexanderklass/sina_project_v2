import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
    //Remove Command for GitHub Pages
    /*
    output: 'export',
    basePath: '/sina_project',
    assetPrefix: '/sina_project',

     */
};

export default nextConfig;
